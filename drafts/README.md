# drafts/

Prose that is **not on the site**. Nothing here is listed in `content.json`, so
nothing here renders. Same principle as `articles/origin-of-ideas.json`: kept on
purpose, wired in on purpose, and not before.

Markdown is the working format. Converting a draft to `articles/<slug>.json` is
the act of publishing it — one `text` section per paragraph, `heading` for each
`##` — so it happens when Aaron says the piece is ready, not as tidying.

| File | Words | ~Read | State |
|------|-------|-------|-------|
| `least-skilled-person-on-site.md` | 1785 | 8 min | Needs work before posting |
| `widening-the-neck.md`            | 1115 | 5 min | Needs work before posting |

These two are a pair, and both descend from the published `good-enough`
(hegemonica, the limiting factor):

- `least-skilled-person-on-site` answers it. `good-enough` says find the limiting
  factor; this one opens with "excellent advice, if you can see the constraint"
  and argues you find it by touching cheap things, not by thinking harder.
- `widening-the-neck` follows that one — it says so itself: "The method is the one
  I've written about elsewhere." It also calls back to the immune-system section
  of the first: "That's the immune system again, this time wearing a business case."

So the reading order is `good-enough` → `least-skilled-person-on-site` →
`widening-the-neck`. If they go up, they want that order, and the middle one has
to be posted before the last one's "elsewhere" resolves to anything.

**Known overlap to resolve first:** `widening-the-neck`'s "Multiply before you
redesign" argues the same thing as the Colony Solution section of the published
`oven-problem` — don't build a bigger module, build a second one, because
replicating something that works beats redesigning it. `oven-problem` already has
the ants, the capacity numbers and the redundancy case. What `widening-the-neck`
adds is new and worth keeping: two stations give you *comparison*, so the
duplicate pays a second time as a free experiment. Either cut back to that
addition and cite `oven-problem`, or accept the restatement deliberately.

Category for both, when they go: **Strategy** — they're method, not economics.
